% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog.R
\name{question_data}
\alias{question_data}
\title{Fetch and decode one question's respondent-level data}
\usage{
question_data(question, years = NULL, decode = TRUE)
}
\arguments{
\item{question}{A question code from \code{\link{question_catalog}}, e.g.
\code{"bri_econ_013"} or \code{"vote_choice"}.}

\item{years}{Optional integer vector to filter \code{year} to.}

\item{decode}{If \code{TRUE} (default), decode \code{response} and the
demographic/vote columns into labeled factors; if \code{FALSE}, return the
raw numeric codes as stored in the Parquet file.}
}
\value{
A data frame with one row per respondent-question-wave: \code{response},
\code{weight}, \code{year}, \code{response_id}, \code{survey_id}, and the columns in
\code{\link{cvpa_grouping_vars}}.
}
\description{
Reads the locally cached \code{resp/question=<question>/data_0.parquet} file
(see \code{\link{update_cvpa_data}}) and, by default, decodes \code{response} and every
demographic/vote-grouping column (see \code{\link{cvpa_grouping_vars}}) into labeled
factors. Works identically for a policy question and for
\code{"vote_choice"}/\code{"vote_intention"} -- vote is just one more question.
}
\examples{
\dontrun{
update_cvpa_data(questions = "bri_econ_013")
d <- question_data("bri_econ_013")
wtd_response(d, grouping_vars = "gender")
}
}
